# Base endpoint for Azure Translator
AZURE_ENDPOINT=https://api.cognitive.microsofttranslator.com

# If your resource is a multi-service (global) cognitive service, set region
# AZURE_REGION=eastus

# Comma-separated subscription keys
AZURE_KEYS=key1,key2,key3

# Local port
PORT=3000

# Optional: set a shared token to protect the proxy
# Requests must include header `x-proxy-token: <this>` or `Authorization: Bearer <this>`
# or provide `?token=<this>` in query string
# PROXY_TOKEN=your-shared-secret
